// @flow
import * as React from 'react'
import { View } from 'react-native'
import * as screenStyles from './item-separator.styles'

const ItemSeparator = () => {
  return <View style={screenStyles.ROOT} />
}

export default ItemSeparator
